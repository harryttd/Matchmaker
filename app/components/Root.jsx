import React from 'react';
// import Navbar from './Navbar';
// import Footer from './Footer';

// { console.log(children)}
const Root = ({ children }) => (
  <div>
    { children }
  </div>
);

export default Root;
